version: '2'

services:
  sc-pootle:
    restart: always
    env_file:
      - environment_prod.yml

  sc-pootle-mysql:
    restart: always
    env_file:
      - environment_prod.yml
    
  sc-pootle-redis:
    restart: always
